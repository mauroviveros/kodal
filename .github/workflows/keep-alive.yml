name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 12 */2 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit health endpoint
        run: curl -s -o /dev/null -w "%{http_code}" https://www.kodal.pet/api/health
