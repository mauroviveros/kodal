---
import type { HTMLTag, Polymorphic } from "astro/types";
import { tv } from "tailwind-variants";

type Props<Tag extends HTMLTag> = Polymorphic<{ as: Tag }> & {
  /**
   * Whether the item is inset (has left padding)
   */
  inset?: boolean;
  /**
   * Whether the item is disabled
   */
  disabled?: boolean;

  /**
   * The variant of the dropdown item
   */
  variant?: "default" | "destructive";
};

export const dropdownItem = tv({
  base: [
    "relative flex cursor-default items-center gap-2 rounded-sm px-2 py-1.5 transition-colors outline-none select-none",
    "data-[disabled]:pointer-events-none data-[disabled]:opacity-50",
    "group/dropdown-item [&>svg]:size-4 [&>svg]:shrink-0",
  ],
  variants: {
    variant: {
      default: "focus:bg-accent focus:text-accent-foreground",
      destructive: "text-destructive focus:bg-destructive/5",
    },
    inset: {
      true: "pl-8",
    },
    disabled: {
      true: "pointer-events-none opacity-50",
    },

  },
  defaultVariants: {
    variant: "default",
    inset: false,
    disabled: false,
  },
});

const { class: className, variant = "default", inset = false, disabled = false, as: Tag = "div", ...rest } = Astro.props;
---

<Tag
  class={dropdownItem({ variant, inset, disabled, class: className })}
  role="menuitem"
  tabindex={disabled ? "-1" : "0"}
  data-disabled={disabled ? "true" : undefined}
  data-slot="dropdown-item"
  {...rest}
>
  <slot />
</Tag>
